
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Library</title>
     <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}"/>
</head>
<body>
    
{% block header %}    
<header>
    <h1>Welcome to the library!</h1>
        <nav>
            <ul>
                <li><a href="/books">Home</a></li>
            </ul>
        </nav>
</header>

{% endblock header %}

    
<section class="table--container">
<table>
<tr>
    <th>Title</th>
    <th>Author</th>
    <th>Genre</th>
    <th></th>
</tr>

{% for book in books  %}
<tr>
    <td><a href="http://127.0.0.1:4999/books/{{books.index(book)}}">{{ book.title }}</a></td>
    <td>{{ book.author }}</td>
    <td>{{ book.genre }}</td>
     <td>
        <form action="/books/delete/{{ book.title }}" method="POST">
            <input type="submit" value="Delete">
        </form>
    </td>
</tr>   

{% endfor %}

</table>  
</section>  

<form method="post" action="/books" class="add__book">
    <h2>Add a new book!</h2>
    
    <div class="inputs">
    <label for="title">Title</label>
    <input type="text" name="title" id="title" placeholder="Enter the book title"/>
    </div>

    <div class="inputs">
    <label for="author">Author</label>
    <input  type="text" for="author" id="author" name="author" placeholder="Enter the author"/>
    </div>

    <div class="inputs">
    <label for="genre">Genre</label>
    <input type="text" for="genre" name="genre" id="genre" placeholder="Enter a genre"/>
    </div>
    
    <input type="submit" value="Add Book">

</form>

    
</body>
</html>